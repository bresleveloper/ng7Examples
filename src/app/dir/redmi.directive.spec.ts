import { RedmiDirective } from './redmi.directive';

describe('RedmiDirective', () => {
  it('should create an instance', () => {
    const directive = new RedmiDirective();
    expect(directive).toBeTruthy();
  });
});
